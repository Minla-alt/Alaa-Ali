{
  "name": "bilingual-educational-platform",
  "version": "1.0.0",
  "description": "A monorepo for bilingual educational platform with React frontend and Node.js/Express backend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev:frontend": "yarn workspace frontend dev",
    "dev:backend": "yarn workspace backend dev",
    "build:frontend": "yarn workspace frontend build",
    "build:backend": "yarn workspace backend build",
    "lint": "yarn workspace frontend lint && yarn workspace backend lint",
    "format": "yarn workspace frontend format && yarn workspace backend format",
    "install:all": "yarn install"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.22.0"
  },
  "license": "MIT"
}